source("renv/activate.R")

Sys.setenv(PROJ_LIB="/usr/local/Cellar/proj/9.3.1/share/proj") # Make sure to change this to the path where you have the proj directory

repos <- c(CRAN = "https://cloud.r-project.org", stan = "https://mc-stan.org/r-packages/")
options(repos = repos)

# Sys.setenv(XML_CONFIG = "/usr/local/opt/libxml2/bin/xml2-config")
